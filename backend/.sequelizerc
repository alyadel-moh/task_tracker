const path = require("path");

module.exports = {
  config: path.resolve(__dirname, "dist", "src", "config", "config.js"),
  "models-path": path.resolve(__dirname, "dist", "src", "models"),
  "migrations-path": path.resolve(__dirname, "dist", "migrations"),
  "seeders-path": path.resolve(__dirname, "dist", "seeders"),
};
